{
  "name": "cbc-provenance-contracts",
  "version": "1.0.0",
  "description": "CBC-Provenance Smart Contracts for DID Registry",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network arbitrumSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "hardhat": "^2.19.0",
    "ethers": "^6.7.0",
    "dotenv": "^16.3.1"
  }
}
